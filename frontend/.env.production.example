# Used only for native (Capacitor) builds and any deployment where the frontend is not
# served from the same origin as the API. Set this to your deployed backend's public URL
# before running `npm run build` for Android/iOS — see docs/MOBILE.md.
VITE_API_BASE_URL=https://api.your-domain.com/api
